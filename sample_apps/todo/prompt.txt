Build a todo list app. At the top, place a TextField for entering new items (placeholder: "Add a task...") bound to /newTask, with an "Add" primary button next to it in a Row. Below that, show a List of todo items. Each item should be a Row containing a CheckBox (bound to /todos/{index}/done) and a Text showing the task name. Include a few sample items in the data model. At the bottom, show a Text with a caption variant showing the count of remaining items.