Build a theme switcher demo app. Show a Card at the top with the title "Appearance" containing a Row of two Buttons: "Light Mode" (secondary) and "Dark Mode" (primary). Below that, show several sample UI elements so the theme effect is visible: a Card titled "Profile" with a Text name and caption email, a Card titled "Settings" containing a CheckBox and a Slider, and a Card titled "Notes" with a TextField. Use setTheme to start in dark mode. Each button uses a serverAction to switch theme. Window 500x500 with dark background.