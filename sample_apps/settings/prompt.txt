Build a settings panel with multiple sections in a Column. First section "Appearance": a ChoicePicker for theme (Light, Dark, System) bound to /settings/theme, and a Slider for font size (min 10, max 24, step 1) bound to /settings/fontSize with a Text label showing the current value. Second section "Notifications": CheckBoxes for "Email notifications" bound to /settings/emailNotify and "Push notifications" bound to /settings/pushNotify. Third section "Account": a DateTimeInput for "Member since" with enableDate true bound to /settings/memberSince. Use Cards to group each section with appropriate titles. Add a "Save Settings" primary button at the bottom.